name: association

on:
  push:

jobs:
  canal:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        sew: ["echo", "contribute", "civilian", "democracy", "recreation"]
        crack: ["hot", "multicultural", "interference", "lawyer"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar aV.jar
